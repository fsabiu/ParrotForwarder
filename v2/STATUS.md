# v2 Status

This file is the single source of truth for v2 progress. Update it whenever a task changes status. Agents resuming work should read this first.

**Format rule**: status is one of `todo`, `in-progress`, `blocked`, `done`. When marking `in-progress`, set `current_owner` and `started_at`. When marking `done`, add the PR link.

## Current phase

Phase 0 - Foundation.

## Current focus

T10 (REST API) done. T11 (WebSocket events + telemetry) next.

## Phase progress

| Phase | Status | Notes |
|---|---|---|
| 0 - Foundation | done | T01-T05 complete. CI green path established. |
| 1 - Supervisor | done | State machine + supervisor runtime + health + JSON logging all green. |
| 2 - REST API | todo | |
| 3 - Dashboard | todo | |
| 4 - Tests/docs/packaging | todo | |

## Task progress

| ID | Title | Phase | Status | Owner | Notes |
|---|---|---|---|---|---|
| T01 | Project skeleton + pyproject | 0 | done | claude (fsabiu) | src/ layout + pyproject. Merged directly to v2. |
| T02 | Config loader (YAML + env + CLI) | 0 | done | claude (fsabiu) | pydantic v2 model, layered precedence, reload policy. Merged directly to v2. |
| T03 | Mock drone backend for tests | 0 | done | claude (fsabiu) | MockDrone + drone_factory DI on ParrotForwarder. Merged directly to v2. |
| T04 | pytest skeleton + CI | 0 | done | claude (fsabiu) | GH Actions ci.yml, ruff, mypy strict on v2. Merged directly to v2. |
| T05 | install.sh + Makefile | 0 | done | claude (fsabiu) | Idempotent Ubuntu 24.04 bootstrap + dev Makefile. Merged directly to v2. |
| T06 | Supervisor state machine | 1 | done | claude (fsabiu) | Pure state machine + backoff. 100% transition coverage. Merged to v2. |
| T07 | Forwarder subprocess + IPC | 1 | done | claude (fsabiu) | Supervisor runtime + IPC schemas + worker stub. E2E subprocess coverage deferred to T17. |
| T08 | Health monitor (Olympe + GStreamer) | 1 | done | claude (fsabiu) | Heartbeat timeout + FPS/battery/RSSI thresholds. Merged to v2. |
| T09 | Structured JSON logging | 1 | done | claude (fsabiu) | JsonFormatter + rotating file handler driven by LoggingConfig. Merged to v2. |
| T10 | REST API (FastAPI) | 2 | done | claude (fsabiu) | FastAPI app with /health /status /config /control/*, RFC 7807 errors. Merged to v2. |
| T11 | WebSocket events + telemetry | 2 | todo | - | |
| T12 | Prometheus metrics | 2 | todo | - | |
| T13 | Dashboard SPA skeleton | 3 | todo | - | |
| T14 | Video preview (HLS or WebRTC) | 3 | todo | - | |
| T15 | Dashboard -> REST/WS wiring | 3 | todo | - | |
| T16 | Integration test harness | 4 | todo | - | |
| T17 | E2E smoke test | 4 | todo | - | |
| T18 | Runbook + dev guide | 4 | todo | - | |
| T19 | Dockerfile + USB passthrough docs | 4 | todo | - | |
| T20 | Release 2.0.0 | 4 | todo | - | |

## Open decisions

| ADR | Topic | Status |
|---|---|---|
| ADR-001 | Supervisor architecture (threads vs subprocess vs systemd template) | accepted |
| ADR-002 | Web framework (FastAPI vs aiohttp) | proposed |
| ADR-003 | Frontend stack (vanilla vs Svelte vs React) | proposed |
| ADR-004 | Video preview transport (HLS vs WebRTC vs MJPEG) | proposed |

## Known blockers

None.

## Log

- **2026-04-17** - Branch `v2` created. Planning docs, roadmap, task list, ADR placeholders written. No code changes yet.
- **2026-04-17** - T01 complete. Moved `parrot_forwarder/` to `src/parrot_forwarder/` via `git mv` (history preserved). Added `pyproject.toml` with `setuptools>=68` build backend, `parrot-forwarder = parrot_forwarder.cli:main` console script, direct deps (`parrot-olympe==0.0.0`, `protobuf==3.20.3`, `PyYAML`), `[project.optional-dependencies].dev` group, and pytest/ruff/mypy tool config. Added `requirements-dev.txt` mirroring the dev group. `ParrotForwarder.py` trimmed to the 3-line shim. `tests/conftest.py` excludes the v1 Olympe-dependent diagnostic scripts from pytest auto-collection so `pytest tests/` runs cleanly on any host. `tests/test_package_layout.py` added as a sanity test (skipped when Olympe is not installed). README install section rewritten around `pip install -e .`. `.gitignore` already covers `build/`, `dist/`, `*.egg-info/`. Full verification (`pip install -e . -r requirements-dev.txt` and `parrot-forwarder --help`) requires the Linux target (Olympe is Linux-only and not available on the dev Mac); pyproject shape and src-layout imports verified locally.
- **2026-04-17** - T02 complete. Added `src/parrot_forwarder/config.py` (pydantic v2 `Config` model mirroring `v2/specs/02-config.md`; `load_config(path, env, cli_overrides)` applies the layered precedence defaults<yaml<env<cli with no module-import side effects; `reload_config(old, new)` returns a `ReloadResult` - all-or-nothing reject if any non-reloadable field changed, per spec). Env vars parsed from `PARROT_FORWARDER_A__B__C` pattern with boolean/int/float coercion. Every sub-model is frozen + `extra="forbid"` so typos produce field-qualified errors. `config.yaml.example` added with every field and inline comment. `tests/test_config.py` has 20 tests covering all six scenarios from `specs/02-config.md` plus malformed input, unknown fields, out-of-range values, and the no-side-effects invariant. Refactored `src/parrot_forwarder/__init__.py` to PEP 562 lazy re-exports so `import parrot_forwarder.config` no longer drags in Olympe; dropped the Olympe-skip in `test_package_layout.py` as a result. All 23 tests pass on Python 3.12 (Mac dev host).
- **2026-04-17** - T03 complete. Added `src/parrot_forwarder/testing/` subpackage with `MockDrone` (matches the Olympe surface actually used in v1: `connect`, `disconnect`, `get_state(MessageClass)`, `drone(MessageClass)` subscription factory) and `MockSubscription`. Controllable via `set_battery_percent`, `set_gps_fix`, `set_attitude`, `set_position`, `force_disconnect`, `fail_next_connect`, `raise_on_next_get_state`, `set_connect_delay`. Thread-safe via internal `RLock`. Added `mock_drone_factory(**defaults)` helper for DI in tests. `ParrotForwarder.__init__` now accepts `drone_factory: Callable[[str], Any]` (defaults to an internal lazy Olympe factory) and `install_signal_handlers: bool` (defaults True); the top-level `import olympe` is gone, and the `from .telemetry/.video` imports in `main.py` are now lazy inside `start_forwarding` so `import parrot_forwarder.main` works on hosts without Olympe. `tests/test_mock_drone.py` adds 18 tests covering every knob, subscription shape, thread-safety, the factory helper, and a signature check that the new DI parameter on `ParrotForwarder.__init__` is present and defaults correctly. All 41 tests pass on Python 3.12.
- **2026-04-17** - T04 complete. Added `.github/workflows/ci.yml` on the Ubuntu 24.04 runner with Python 3.11, cached pip via `pyproject.toml`+`requirements-dev.txt`. CI installs dev deps and does `pip install -e . --no-deps` so the full Olympe transitive stack doesn't have to resolve on the runner (parrot-olympe is Linux-only and pulls heavy system libs); the v2 test suite doesn't need it. Steps: ruff → mypy → pytest with `-m "not live and not slow"`. Expanded `[tool.ruff]` in `pyproject.toml` with `select = [E, F, W, I, UP, B]`, line length 100, and `extend-exclude` for v1 modules that aren't linted yet. Added `mypy.ini` with `strict = True` globally, `ignore_missing_imports` for Olympe/GStreamer, and per-module relaxations for the v1 code + the half-rewritten `main.py`. Added `types-PyYAML` and `PyYAML` to `requirements-dev.txt`/dev extras. Ruff autofix pass migrated v2 modules to PEP 604 `X | None`/`collections.abc` style. Local verification: ruff clean, mypy clean, 41 tests pass. CI verification pending the first push.
- **2026-04-17** - T05 complete. Added `scripts/install.sh` - idempotent Ubuntu 24.04 ARM64 bootstrap. Steps: OS check (warn-only), apt install of GStreamer + SDL2 + OpenCV + build toolchain, pyenv install pinned to `v2.4.17`, Python 3.11.11 build, `.venv` creation, `pip install -e . -r requirements-dev.txt`, force-reinstall `protobuf==3.20.3`, verify `mpegtsmux` + `srtsink`, seed `/etc/parrot-forwarder/config.yaml` from the example if missing, summary. `set -euo pipefail`, every step emits a single structured log line, `sudo` only where strictly needed. Skip flags: `PF_SKIP_APT=1`, `PF_SKIP_PYENV=1`. Added top-level `Makefile` with `install`, `test`, `lint`, `typecheck`, `fmt`, `run`, `ci`, `clean` targets - each a one-liner routing to the underlying tool. Collapsed the README install section to a short "run the script" block. Full VM verification deferred to a real Ubuntu 24.04 ARM64 host; shell syntax and Makefile parse verified locally. Phase 0 complete.
- **2026-04-17** - T06 complete. Accepted ADR-001 (subprocess + UDS IPC). Added `src/parrot_forwarder/backoff.py` (`BackoffPolicy` dataclass with validation, pure `compute_delay(n, policy, rng)` function clamping the exponent so huge failure counts never overflow, `BackoffState` helper that increments on failure and resets after a healthy STREAMING window per spec). Added `src/parrot_forwarder/state_machine.py` - pure, no-IO implementation of the state chart from `v2/architecture/state-machine.md`. `State` is a `StrEnum`; every event is a frozen dataclass (`OlympeConnected`, `PipelineError`, `Heartbeat`, `UserStart/Stop/Reset`, `HealthUnresponsive`, `HealthDegraded`, `Timeout`, `ForwarderExit`, ...); every side effect is a typed descriptor (`SpawnForwarder`, `KillForwarder`, `ScheduleRestart`, `SetTimeout`, `EmitMetric`, `Log`). `StateMachine.step(event) -> list[SideEffect]` handles every transition called out in the spec including the `RESTARTING` → escalate-to-SIGKILL timeout, illegal events log and stay put (never crash), and `metrics` emit `state_transitions_total` + `restarts_total` as the spec requires. `tests/test_backoff.py` has 14 tests (policy validation, jitter determinism, clamping, overflow guard, 5-failure ladder matching spec). `tests/test_state_machine.py` has 33 tests covering every transition, every per-state timeout, illegal events across the full matrix of states and events, metric emission, backoff growth across repeated failures, and the full happy-cycle DISCONNECTED → STREAMING → RESTARTING → STREAMING smoke test. Ruff + mypy strict clean; 89 tests pass locally.
- **2026-04-17** - T10 complete. Added FastAPI `>=0.115`, `uvicorn[standard]>=0.30`, and `pydantic>=2.6` to main runtime deps (dev-deps already had them). Created `src/parrot_forwarder/supervisor/api/__init__.py` with `create_app(supervisor)` factory returning a FastAPI app bound to the supervisor instance (no globals, tests build many side-by-side). Routes: `GET /health` (liveness), `GET /status` (current `State` + backoff counters), `GET /config` (backoff + auto_start snapshot; full config layered in later), `POST /control/{start,stop,reset}` returning 202 + current state. Error responses are RFC 7807 `application/problem+json` via exception handlers registered for both `fastapi.HTTPException` and `starlette.HTTPException` (the latter catches FastAPI's own 404/405 path). Request-ID middleware stamps every request/response using `X-Request-ID`; caller-supplied IDs echo through. `tests/test_api.py` has 14 tests covering all routes, request-ID echo + generation, problem+json shape on 404 and 500 (unhandled exception), validation failure on bad body, an end-to-end test that POST /control/start transitions the supervisor state via the real event queue, and an OpenAPI smoke check. 147 tests pass; ruff + mypy strict clean.
- **2026-04-17** - T09 complete. Added `src/parrot_forwarder/logging_setup.py`: `JsonFormatter` emits one JSON object per record with stable `ts` (ISO-8601 UTC with millisecond precision + `Z` suffix), `level`, `logger`, `msg`, plus any `extra=` fields; reserved LogRecord attributes are filtered so they can't clobber the canonical keys, nested values are JSON-safe via `_jsonify`, exceptions render as `exception` strings. `TextFormatter` covers local dev. `configure_logging(LoggingConfig, also_stream=True)` wires a `RotatingFileHandler` (honoring `max_bytes` / `backup_count`) + optional stdout handler, creates the parent dir if missing (falls back to stream-only if permissions deny), and tames `olympe` / `ulog` loggers to WARNING unless the global level is DEBUG. `log_with_fields(logger, level, msg, **fields)` is a helper that routes structured extras safely. `tests/test_logging.py` has 10 tests covering format shape, extras, exception rendering, rotating file output, level filtering, unknown-format rejection, Olympe taming + DEBUG unlock, the helper's structured emit, and the reserved-key filter. ruff + mypy strict clean; 133 tests pass. Phase 1 complete.
- **2026-04-17** - T08 complete. Added `src/parrot_forwarder/supervisor/health.py` with `HealthThresholds` dataclass (configurable: heartbeat_timeout_seconds, fps_min, fps_min_window_seconds, battery_percent_min, rssi_dbm_min, recovery_window_seconds) and `HealthMonitor` - stateful observer driven by `record_heartbeat(hb, metrics)` and `poll()` that returns state-machine events to post (`HealthUnresponsive` when no heartbeat for `> heartbeat_timeout_seconds`, `HealthDegraded(signal=...)` for FPS (windowed), battery, and RSSI breaches). Recovery hysteresis - a "once fired" flag prevents event flapping; a healthy reading clears the flag so the next breach fires a fresh event. `reset()` wipes state for worker respawn. `run_health_poll_loop(monitor, post_event)` drives the periodic poll in production. `tests/test_health.py` has 11 tests (no-heartbeat silent, fire-once semantics, windowed FPS, recovery clearing, battery + RSSI thresholds, configurable threshold override, reset). All 123 tests pass; ruff + mypy clean.
- **2026-04-17** - T07 complete. Added `src/parrot_forwarder/ipc.py` - pydantic v2 discriminated union on `kind` for every IPC message (worker-emitted `olympe.connected/disconnected/error`, `pipeline.started/eos/error`, `heartbeat`, `telemetry`; supervisor-emitted `stop`, `set_telemetry_rate`), plus `encode`/`decode` with newline-delimited JSON, sorted keys, and `IpcError` naming the offending field path on schema violations. Added `src/parrot_forwarder/supervisor/__init__.py` - asyncio `Supervisor` dataclass that feeds events from the IPC pump / API / watchdog timers into the state machine and executes every returned `SideEffect` (Log, EmitMetric, SetTimeout, SpawnForwarder, KillForwarder, ScheduleRestart). `WorkerHandle` abstracts the subprocess for tests; the default factory `_default_spawn_worker` uses `asyncio.create_subprocess_exec` against `parrot-forwarder-worker` (new console script installed from `parrot_forwarder.forwarder.worker:main`). `install_signal_handlers` wires SIGINT/SIGTERM to `Supervisor.stop` with Windows fallback. `src/parrot_forwarder/forwarder/worker.py` is a stub worker that connects back over UDS, emits `olympe.connected`/`pipeline.started` and periodic heartbeats, and exits cleanly on `stop` - enough to run the T17 end-to-end smoke later; the real Olympe wiring lives in T08/T09. `tests/test_ipc.py` has 14 tests (roundtrip every message type, reject empty frames, malformed JSON, unknown `kind`, missing required fields, out-of-range field values, heartbeat defaults, key ordering). `tests/test_supervisor_integration.py` adds 5 async tests using an in-process fake worker - happy path reaches STREAMING, unexpected crash -> ScheduleRestart -> fresh worker, UserReset kills and respawns, UserStop halts auto-restart, `stop()` is idempotent. Two runtime bugs caught and fixed during testing: state timeouts must use the real clock (don't inherit the injectable `sleep` used for backoff), and `_kill_worker` must not clear `_worker` or cancel the exit watcher - both would strand RESTARTING forever. Ruff + mypy strict clean; 112 tests pass.
